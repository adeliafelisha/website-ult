@extends('layouts.app') @section('title',$article->title) @section('description',$article->seo_description?:$article->excerpt) @section('content')<div class="container breadcrumb"><a href="{{ route('home') }}">Beranda</a><span>/</span><a href="{{ route('articles.index') }}">Artikel</a><span>/</span><span>{{ $article->title }}</span></div><article class="article-detail"><header><span class="eyebrow">{{ $article->category }}</span><h1>{{ $article->title }}</h1><p>{{ $article->excerpt }}</p><small>{{ optional($article->published_at)->translatedFormat('d F Y') }} · {{ $article->author }}</small></header><img class="cover" src="{{ $article->featured_image?asset('storage/'.$article->featured_image):asset('images/kehidupan-mahasiswa.jpg') }}" alt=""><div class="prose rich">{!! $article->content !!}</div><footer>Terakhir diperbarui {{ $article->updated_at->translatedFormat('d F Y') }} · Pemilik konten: {{ $article->content_owner ?: 'ULT Unpad' }}</footer></article>@endsection
