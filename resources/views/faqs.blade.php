@extends('layouts.app') @section('title',__('ui.faq_title')) @section('content')<section class="page-hero faq-hero"><div class="container"><span class="eyebrow">{{ __('ui.campus_guide') }}</span><h1>{{ __('ui.faq_title') }}</h1><p>{{ __('ui.faq_search_help') }}</p><form class="faq-finder" method="get"><div class="search-box"><input name="q" value="{{ request('q') }}" placeholder="{{ __('ui.faq_search_placeholder') }}" aria-label="{{ __('ui.faq_search_placeholder') }}"><button>{{ __('ui.search_faq') }}</button></div><label>{{ __('ui.topic') }}<select name="category"><option value="">{{ __('ui.all_topics') }}</option>@foreach($faqCategories as $category)<option value="{{ $category }}" @selected(request('category')===$category)>{{ $category }}</option>@endforeach</select></label></form>@if(request()->hasAny(['q','category']))<div class="active-search"><span>{{ $faqs->total() }} {{ __('ui.answers_found') }}</span><a href="{{ route('faqs') }}">{{ __('ui.clear_search') }}</a></div>@endif</div></section><section class="section"><div class="container narrow"><div class="faq-layout"><aside><strong>{{ __('ui.browse_topic') }}</strong><a class="@if(!request('category')) active @endif" href="{{ route('faqs',request()->only('q')) }}">{{ __('ui.all_topics') }}</a>@foreach($faqCategories as $category)<a class="@if(request('category')===$category) active @endif" href="{{ route('faqs',array_filter(['q'=>request('q'),'category'=>$category])) }}">{{ $category }}</a>@endforeach</aside><div class="accordion">@forelse($faqs as $faq)<details><summary><span><small>{{ $faq->category }}</small>{{ $faq->question }}</span><b>+</b></summary><div>{!! $faq->answer !!}@if($faq->external_url)<div class="faq-link"><a class="btn external-link" data-track="{{ $faq->question }}" href="{{ $faq->external_url }}" target="_blank" rel="noopener">{{ $faq->external_label ?: __('ui.open_related_link') }} ↗</a></div>@endif</div></details>@empty<div class="empty-state"><h2>{{ __('ui.answer_empty') }}</h2><p>{{ __('ui.try_other') }}</p><a class="btn" href="{{ route('faqs') }}">{{ __('ui.clear_search') }}</a></div>@endforelse</div></div>{{ $faqs->links() }}</div></section>@endsection
