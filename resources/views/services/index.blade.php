@extends('layouts.app') @section('title',__('ui.service_directory_title')) @section('content')
<section class="page-hero"><div class="container"><span class="eyebrow">{{ __('ui.service_directory_eyebrow') }}</span><h1>{{ __('ui.service_directory_heading') }}</h1><p>{{ __('ui.service_directory_text') }}</p></div></section>
<section class="section service-categories"><div class="container"><form class="finder-panel keyword-finder" method="get"><div class="finder-search"><label for="service-query">{{ __('ui.what_service') }}</label><div class="search-box"><input id="service-query" name="q" value="{{ request('q') }}" placeholder="{{ __('ui.service_search_placeholder') }}"><button>{{ __('ui.search') }}</button></div></div>@if(request('q'))<a href="{{ route('services.index') }}">{{ __('ui.clear_search') }}</a>@endif</form>@if(request('q'))<div class="service-search-results"><div class="results-summary"><p><strong>{{ $services->total() }}</strong> {{ __('ui.services_found') }}</p><span>{{ __('ui.result_for') }} “{{ request('q') }}”</span></div><div class="card-grid">@forelse($services as $service)<x-service-card :service="$service"/>@empty<div class="empty-state"><h2>{{ __('ui.service_empty') }}</h2><p>{{ __('ui.service_empty_text') }}</p></div>@endforelse</div>{{ $services->links() }}</div>@endif<div class="section-head service-groups-head"><div><span class="eyebrow">{{ __('ui.choose_category') }}</span><h2>{{ __('ui.service_groups_title') }}</h2><p>{{ __('ui.service_groups_text') }}</p></div></div><div class="service-category-grid">@foreach($categories as $index => $category)<a class="service-category-card" href="{{ route('services.category',$category) }}"><span>{{ str_pad($index + 1, 2, '0', STR_PAD_LEFT) }}</span><div><h3>{{ $category->name }}</h3><p>{{ $category->description }}</p></div><b aria-hidden="true">→</b></a>@endforeach</div></div></section>@endsection
