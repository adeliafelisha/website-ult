<article class="card service-card"><span class="eyebrow">{{ $service->category?->name }}</span><h3><a href="{{ route('services.show',$service) }}">{{ $service->title }}</a></h3><p>{{ $service->summary }}</p><div class="card-meta"><span>{{ ['online'=>'Daring','offline'=>'Luring','hybrid'=>'Hybrid'][$service->delivery_type] }}</span><a href="{{ route('services.show',$service) }}">Lihat layanan →</a></div></article>
