@extends('layouts.app') @section('title',__('ui.service_directory_title')) @section('content')
<section class="page-hero"><div class="container"><span class="eyebrow">{{ __('ui.service_directory_eyebrow') }}</span><h1>{{ __('ui.service_directory_heading') }}</h1><p>{{ __('ui.service_directory_text') }}</p></div></section>
<section class="section service-finder"><div class="container"><form class="finder-panel" method="get"><div class="finder-search"><label for="service-query">{{ __('ui.what_service') }}</label><div class="search-box"><input id="service-query" name="q" value="{{ request('q') }}" placeholder="{{ __('ui.service_search_placeholder') }}"><button>{{ __('ui.search') }}</button></div></div><fieldset><legend>{{ __('ui.refine_results') }}</legend><div class="filter-grid"><label>{{ __('ui.category') }}<select name="category"><option value="">{{ __('ui.all_categories') }}</option>@foreach($categories as $category)<option value="{{ $category->slug }}" @selected(request('category')===$category->slug)>{{ $category->name }}</option>@endforeach</select></label><label>{{ __('ui.audience') }}<select name="audience"><option value="">{{ __('ui.all_users') }}</option>@foreach(['Mahasiswa Baru','Mahasiswa Aktif','Lulusan','Dosen & Tendik','Internasional','Masyarakat Umum'] as $a)<option @selected(request('audience')===$a)>{{ $a }}</option>@endforeach</select></label><label>{{ __('ui.channel') }}<select name="delivery"><option value="">{{ __('ui.all_channels') }}</option>@foreach(['online','offline','hybrid'] as $delivery)<option value="{{ $delivery }}" @selected(request('delivery')===$delivery)>{{ __('ui.'.$delivery) }}</option>@endforeach</select></label></div></fieldset><div class="finder-actions"><button class="btn">{{ __('ui.apply_filter') }}</button><a href="{{ route('services.index') }}">{{ __('ui.clear_all') }}</a></div></form><div class="results-summary"><p><strong>{{ $services->total() }}</strong> {{ __('ui.services_found') }}</p>@if(request()->hasAny(['q','category','audience','delivery']))<span>{{ __('ui.filtered_results') }}</span>@endif</div><div class="card-grid">@forelse($services as $service)<x-service-card :service="$service"/>@empty<div class="empty-state"><h2>{{ __('ui.service_empty') }}</h2><p>{{ __('ui.service_empty_text') }}</p><a class="btn" href="{{ route('services.index') }}">{{ __('ui.clear_all') }}</a></div>@endforelse</div>{{ $services->links() }}</div></section>@endsection
