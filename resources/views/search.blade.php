@extends('layouts.app') @section('title',__('ui.global_search')) @section('content')<section class="page-hero"><div class="container"><span class="eyebrow">{{ __('ui.global_search') }}</span><h1>{{ $term ? __('ui.result_for').' “'.$term.'”' : __('ui.search_info') }}</h1><form class="hero-search small"><input name="q" value="{{ $term }}" minlength="2" placeholder="{{ __('ui.minimum_chars') }}"><button>{{ __('ui.search') }}</button></form></div></section><section class="section"><div class="container search-results">@php($total=$services->count()+$articles->count()+$faqs->count())@if($term&&$total===0)<div class="empty-state"><h2>{{ __('ui.no_results') }}</h2><p>{{ __('ui.no_results_text') }}</p></div>@endif @if($services->isNotEmpty())<h2>{{ __('ui.services') }} <span>{{ $services->count() }}</span></h2>@foreach($services as $item)<a class="result" href="{{ route('services.show',$item) }}"><small>{{ __('ui.services') }}</small><h3>{{ $item->title }}</h3><p>{{ $item->summary }}</p></a>@endforeach @endif @if($articles->isNotEmpty())<h2>{{ __('ui.articles') }} <span>{{ $articles->count() }}</span></h2>@foreach($articles as $item)<a class="result" href="{{ route('articles.show',$item) }}"><small>{{ __('ui.articles') }} · {{ $item->category }}</small><h3>{{ $item->title }}</h3><p>{{ $item->excerpt }}</p></a>@endforeach @endif @if($faqs->isNotEmpty())<h2>FAQ <span>{{ $faqs->count() }}</span></h2>@foreach($faqs as $item)<a class="result" href="{{ route('faqs',['q'=>$item->question]) }}"><small>FAQ · {{ $item->category }}</small><h3>{{ $item->question }}</h3></a>@endforeach @endif</div></section>@endsection
