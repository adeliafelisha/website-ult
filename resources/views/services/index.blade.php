@extends('layouts.app') @section('title','Direktori Layanan') @section('content')
<section class="page-hero"><div class="container"><span class="eyebrow">Direktori layanan</span><h1>Temukan layanan sesuai kebutuhan Anda</h1><p>Pelajari persyaratan, prosedur, lokasi, dan kanal resmi sebelum melanjutkan proses.</p></div></section>
<section class="section"><div class="container"><form class="filter-bar" method="get"><select name="category" aria-label="Kategori"><option value="">Semua kategori</option>@foreach($categories as $category)<option value="{{ $category->slug }}" @selected(request('category')===$category->slug)>{{ $category->name }}</option>@endforeach</select><select name="audience" aria-label="Pengguna"><option value="">Semua pengguna</option>@foreach(['Mahasiswa Baru','Mahasiswa Aktif','Lulusan','Dosen & Tendik','Internasional','Masyarakat Umum'] as $a)<option @selected(request('audience')===$a)>{{ $a }}</option>@endforeach</select><button class="btn">Terapkan filter</button><a href="{{ route('services.index') }}">Reset</a></form><div class="card-grid">@forelse($services as $service)<x-service-card :service="$service"/>@empty<div class="empty-state"><h2>Layanan belum ditemukan</h2><p>Coba kategori lain atau gunakan pencarian global.</p></div>@endforelse</div>{{ $services->links() }}</div></section>@endsection
