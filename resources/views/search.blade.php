@extends('layouts.app') @section('title','Hasil Pencarian') @section('content')<section class="page-hero"><div class="container"><span class="eyebrow">Pencarian global</span><h1>{{ $term ? 'Hasil untuk “'.$term.'”' : 'Cari layanan dan informasi' }}</h1><form class="hero-search small"><input name="q" value="{{ $term }}" minlength="2" placeholder="Masukkan minimal 2 karakter"><button>Cari</button></form></div></section><section class="section"><div class="container search-results">@php($total=$services->count()+$articles->count()+$faqs->count())@if($term&&$total===0)<div class="empty-state"><h2>Belum ada hasil yang cocok</h2><p>Coba istilah lain, buka <a href="{{ route('faqs') }}">FAQ</a>, atau <a href="{{ route('contact') }}">hubungi ULT</a>.</p></div>@endif @if($services->isNotEmpty())<h2>Layanan <span>{{ $services->count() }}</span></h2>@foreach($services as $item)<a class="result" href="{{ route('services.show',$item) }}"><small>Layanan</small><h3>{{ $item->title }}</h3><p>{{ $item->summary }}</p></a>@endforeach @endif @if($articles->isNotEmpty())<h2>Artikel <span>{{ $articles->count() }}</span></h2>@foreach($articles as $item)<a class="result" href="{{ route('articles.show',$item) }}"><small>Artikel · {{ $item->category }}</small><h3>{{ $item->title }}</h3><p>{{ $item->excerpt }}</p></a>@endforeach @endif @if($faqs->isNotEmpty())<h2>FAQ <span>{{ $faqs->count() }}</span></h2>@foreach($faqs as $item)<a class="result" href="{{ route('faqs',['q'=>$item->question]) }}"><small>FAQ · {{ $item->category }}</small><h3>{{ $item->question }}</h3></a>@endforeach @endif</div></section>@endsection
