<article class="card service-card"><span class="eyebrow">{{ $service->category?->name }}</span><h3><a href="{{ route('services.show',$service) }}">{{ $service->title }}</a></h3><p>{{ $service->summary }}</p><div class="card-meta"><span>{{ __('ui.'.$service->delivery_type) }}</span><a href="{{ route('services.show',$service) }}">{{ __('ui.view_service') }} →</a></div></article>
